#!/bin/sh
set -e
# Automatically added by dh_pycentral
rm -f /var/lib/pycentral/gdebi-core.pkgremove
if which pycentral >/dev/null 2>&1; then
	pycentral pkginstall gdebi-core
	if grep -qs '^gdebi-core$' /var/lib/pycentral/delayed-pkgs; then
		sed -i '/^gdebi-core$/d' /var/lib/pycentral/delayed-pkgs
	fi
fi
# End automatically added section
