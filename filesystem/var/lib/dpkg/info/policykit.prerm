#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ -x "/etc/init.d/policykit" ] && [ "$1" = remove ]; then
	if [ -x "`which invoke-rc.d 2>/dev/null`" ]; then
		invoke-rc.d policykit stop || exit $?
	else
		/etc/init.d/policykit stop || exit $?
	fi
fi
# End automatically added section
