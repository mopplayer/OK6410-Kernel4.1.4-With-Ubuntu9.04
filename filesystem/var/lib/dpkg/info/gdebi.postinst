#!/bin/sh
set -e
# Automatically added by dh_pycentral
rm -f /var/lib/pycentral/gdebi.pkgremove
if which pycentral >/dev/null 2>&1; then
	pycentral pkginstall gdebi
	if grep -qs '^gdebi$' /var/lib/pycentral/delayed-pkgs; then
		sed -i '/^gdebi$/d' /var/lib/pycentral/delayed-pkgs
	fi
fi
# End automatically added section
